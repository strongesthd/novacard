"use client";
import { ChangeEvent, useEffect, useState } from "react";
type Job = { id: string; status: string; result?: Record<string, unknown>; error?: string };
export default function OcrPanel() { const [job,setJob]=useState<Job|null>(null); const [busy,setBusy]=useState(false); const [message,setMessage]=useState("");
  useEffect(() => { if (!job || ["succeeded","failed"].includes(job.status)) return; const timer=window.setInterval(async()=>{ const token=localStorage.getItem("novacard_token"); const r=await fetch(`/api/jobs/${job.id}`,{headers:{Authorization:`Bearer ${token||""}`}}); if(r.ok)setJob((await r.json()).job); },1500); return()=>window.clearInterval(timer); },[job]);
  const upload=async(e:ChangeEvent<HTMLInputElement>)=>{ const file=e.target.files?.[0]; if(!file)return; if(!["image/jpeg","image/png","image/webp"].includes(file.type)||file.size>10*1024*1024){setMessage("Ch? nh?n ?nh JPEG, PNG ho?c WebP t?i ?a 10MB");return;} setBusy(true);setMessage(""); try { const data=await new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result));r.onerror=()=>reject(new Error("Kh?ng ??c ???c ?nh"));r.readAsDataURL(file)}); const token=localStorage.getItem("novacard_token"); const response=await fetch("/api/ocr/jobs",{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token||""}`},body:JSON.stringify({contentType:file.type,data})}); const body=await response.json(); if(!response.ok)throw new Error(body.error||"Kh?ng th? t?o OCR job");setJob(body.job); }catch(error){setMessage(error instanceof Error?error.message:"Upload th?t b?i")}finally{setBusy(false)}};
  return <section className="panel"><div className="panel-heading"><div><h2>S? h?a danh thi?p gi?y</h2><p>OCR Vi?t ? Anh ? H?n ch?y n?n, kh?ng ch?n m?n h?nh.</p></div></div><label className="upload-drop">{busy?"?ang t?i ?nh?":job?.status==="processing"?"?ang nh?n di?n?":"Ch?n ho?c ch?p ?nh danh thi?p"}<input type="file" accept="image/jpeg,image/png,image/webp" capture="environment" onChange={upload} disabled={busy}/></label>{job&&<p className="form-message">Job {job.id.slice(0,8)}? ? {job.status}{job.status==="succeeded"&&" ? k?t qu? ?? s?n s?ng ?? review"}</p>}{job?.status==="succeeded"&&<div className="ocr-review"><strong>Review tr??c khi l?u</strong>{Object.entries(job.result||{}).map(([key,value])=><label key={key}>{key}<input defaultValue={String(value??"")}/></label>)}<button className="primary-cta" type="button" onClick={async()=>{const token=localStorage.getItem("novacard_token");const r=await fetch(`/api/ocr/jobs/${job.id}/confirm`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token||""}`},body:JSON.stringify({fields:job.result})});setMessage(r.ok?"?? l?u li?n h?.":"Kh?ng th? l?u li?n h?.")}}>X?c nh?n v? l?u</button></div>}{message&&<p className="form-message">{message}</p>}</section> }
