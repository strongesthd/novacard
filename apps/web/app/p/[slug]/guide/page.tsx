import { ArrowLeft, CheckCircle2, FileDown, Smartphone, Sparkles } from "lucide-react";

export default async function VCardGuide({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  return <main className="guide-shell"><nav className="guide-nav"><a className="brand" href="/"><span className="brand-mark"><Sparkles size={16} /></span>NovaCard</a><a className="guide-back" href={`/p/${encodeURIComponent(slug)}`}><ArrowLeft size={15} /> Quay lại hồ sơ</a></nav><section className="guide-card"><div className="guide-icon"><Smartphone size={25} /></div><p className="kicker">Hướng dẫn nhanh</p><h1>Lưu danh bạ trên iPhone</h1><p className="guide-intro">Lưu thông tin liên hệ từ danh thiếp số vào ứng dụng Danh bạ chỉ trong vài bước.</p><div className="guide-steps"><article><span>01</span><div><h2>Tải danh bạ</h2><p>Quay lại hồ sơ và nhấn nút <strong>Lưu vào Danh bạ · vCard</strong>.</p></div></article><article><span>02</span><div><h2>Xác nhận thông tin</h2><p>Trong màn hình nhập Danh bạ, kiểm tra thông tin rồi chọn <strong>Tạo liên hệ mới</strong>.</p></div></article><article><span>03</span><div><h2>Mở tệp nếu cần</h2><p>Nếu Safari tải tệp xuống, mở ứng dụng Tệp và chọn tệp <code>{slug}.vcf</code> để nhập.</p></div></article></div><div className="guide-tip"><CheckCircle2 size={19} /><div><strong>Gợi ý</strong><p>Sau khi lưu, bạn có thể cập nhật hồ sơ mà không cần lưu lại mã QR.</p></div></div><a className="guide-cta" href={`/p/${encodeURIComponent(slug)}`}><FileDown size={17} /> Quay lại và lưu danh bạ</a></section></main>;
}
