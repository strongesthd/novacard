export default async function VCardGuide({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <main style={{maxWidth:560,margin:"0 auto",padding:24}}><h1>Lưu danh bạ trên Safari iOS</h1><ol><li>Nhấn <strong>Lưu vào Danh bạ (vCard)</strong>.</li><li>Trong màn hình nhập Danh bạ, kiểm tra thông tin rồi nhấn <strong>Tạo liên hệ mới</strong>.</li><li>Nếu Safari chỉ tải tệp, mở tệp trong ứng dụng Tệp và nhấn tệp <code>{slug}.vcf</code> để nhập.</li></ol><p><a href={`/p/${encodeURIComponent(slug)}`}>Quay lại hồ sơ</a></p></main>}
